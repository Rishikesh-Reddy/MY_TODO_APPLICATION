<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%= csrfToken %>">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="grid grid-cols-6">
        <div class="col-start-3 col-span-2">
            <%- include('header.ejs') %> 
            <p class="py-2">New Here ? <a href="/signup" class="text-green-600">Sign Up</a></p>
            <h6 class="py-4">Sign In</h6>
            <form action="/session" method="post">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <div class="py-3">
                    <label for="email">Email: </label>
                    <input type="email" name="email" id="email" class="border border-gray-300 gray-900 rounded p-2 w-full" required>
                </div>
                <div class="py-3">
                    <label for="password">Password: </label>
                    <input type="password" name="password" id="password" class="border border-gray-300 gray-900 rounded p-2 w-full" required>
                </div>
                <div class="py-3">
                    <button type="submit" class="text-white bg-green-600 hover:bg-green-800 font-medium rounded px-5 py-2">Sign In</button>
                </div>
            </form>
            <div>
                <% if(messages && messages.error && messages.error.length > 0)  { %>
                    <ul class="bg-red-100 rounded border border-red-200 border-2">
                        <% for(var i=0; i<messages.error.length; i++) { %>
                                <li class="text-center"> <%= messages.error[i] %></li>
                        <% } %>
                    </ul>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>